import { Layout } from "antd";
export { Row, Col } from "antd";

export { Layout as BOLayout } from "antd";
export const { Header } = Layout;
export const { Sider } = Layout;
export const { Content } = Layout;
